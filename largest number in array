#Find the Largest Element in an Array

s = [10, 5, 20, 15, 8]
size = len(s)
ans = 0
for i in range(0,size-1):
     #num = s[i]
     #print(num)
     if s[i] > ans:
         ans = s[i]
     else:
         ans
         
print(ans)

print("####################")
print("####################")

arr = [10, 5, 20, 15, 8]
largest = arr[0]
for num in arr[1:]:       
      if num > largest:
         largest = num

print(largest)    
